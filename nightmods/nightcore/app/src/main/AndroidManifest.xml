<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.instagram.android" />
    </queries>

    <application
        android:allowBackup="true"
        android:description="@string/app_description"
        android:icon="@drawable/ic_night"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NightCore">

        <provider
            android:name=".config.NightCoreSettingsProvider"
            android:authorities="dev.nightmods.core.settings"
            android:exported="true" />

        <receiver
            android:name=".config.NightCoreSettingsReceiver"
            android:exported="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription" android:value="Night cross-app UI capability engine" />
        <meta-data android:name="xposedminversion" android:value="92" />
        <meta-data android:name="xposedscope" android:resource="@array/night_core_scope" />
    </application>
</manifest>
