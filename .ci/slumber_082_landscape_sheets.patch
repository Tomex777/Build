--- slumber081_source/app/build.gradle.kts	2026-09-11 18:15:42.000000000 +0000
+++ slumber082_source/app/build.gradle.kts	2026-09-11 19:26:44.270907018 +0000
@@ -75,8 +75,8 @@
         applicationId = "com.night.pianohub"
         minSdk = 36
         targetSdk = 36
-        versionCode = 13
-        versionName = "0.8.1"
+        versionCode = 14
+        versionName = "0.8.2"
     }
 
     compileOptions {
--- slumber081_source/app/src/main/java/com/night/pianohub/ui/screens/PianoScreen.kt	2026-09-11 18:15:42.000000000 +0000
+++ slumber082_source/app/src/main/java/com/night/pianohub/ui/screens/PianoScreen.kt	2026-09-11 19:26:44.271733612 +0000
@@ -46,6 +46,7 @@
 import androidx.compose.material3.MaterialTheme
 import androidx.compose.material3.ModalBottomSheet
 import androidx.compose.material3.OutlinedButton
+import androidx.compose.material3.rememberModalBottomSheetState
 import androidx.compose.material3.Surface
 import androidx.compose.material3.Switch
 import androidx.compose.material3.Text
@@ -129,6 +130,9 @@
     var showInstrumentSheet by remember { mutableStateOf(false) }
     var showPianoSettingsSheet by remember { mutableStateOf(false) }
     var showTeacherSheet by remember { mutableStateOf(false) }
+    val teacherSheetState = rememberModalBottomSheetState(skipPartiallyExpanded = true)
+    val pianoSettingsSheetState = rememberModalBottomSheetState(skipPartiallyExpanded = true)
+    val instrumentSheetState = rememberModalBottomSheetState(skipPartiallyExpanded = true)
     var pendingExit by remember { mutableStateOf<(() -> Unit)?>(null) }
 
     // Practice is the only forced-landscape destination. Leaving it restores
@@ -669,7 +673,10 @@
     }
 
     if (showTeacherSheet) {
-        ModalBottomSheet(onDismissRequest = { showTeacherSheet = false }) {
+        ModalBottomSheet(
+            onDismissRequest = { showTeacherSheet = false },
+            sheetState = teacherSheetState,
+        ) {
             Column(
                 modifier = Modifier.verticalScroll(rememberScrollState()).padding(horizontal = 20.dp).padding(bottom = 28.dp),
                 verticalArrangement = Arrangement.spacedBy(12.dp),
@@ -695,7 +702,10 @@
     }
 
     if (showPianoSettingsSheet) {
-        ModalBottomSheet(onDismissRequest = { showPianoSettingsSheet = false }) {
+        ModalBottomSheet(
+            onDismissRequest = { showPianoSettingsSheet = false },
+            sheetState = pianoSettingsSheetState,
+        ) {
             Column(
                 modifier = Modifier.padding(horizontal = 20.dp).padding(bottom = 28.dp),
                 verticalArrangement = Arrangement.spacedBy(12.dp),
@@ -773,7 +783,10 @@
     }
 
     if (showInstrumentSheet) {
-        ModalBottomSheet(onDismissRequest = { showInstrumentSheet = false }) {
+        ModalBottomSheet(
+            onDismissRequest = { showInstrumentSheet = false },
+            sheetState = instrumentSheetState,
+        ) {
             Column(
                 modifier = Modifier.padding(horizontal = 20.dp).padding(bottom = 28.dp),
                 verticalArrangement = Arrangement.spacedBy(12.dp),
