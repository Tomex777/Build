name: Runner Diagnostic

on:
  push:
    branches:
      - sora-android-ci
  workflow_dispatch:

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Prove runner starts
        run: |
          echo "runner-started"
          uname -a
          java -version || true
