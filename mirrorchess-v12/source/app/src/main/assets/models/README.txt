Optional development asset location for maia3-5m.fp16.onnx.

Normal users do not need to place anything here: Mirror Chess can download or import the verified Maia model from the in-app Settings screen and stores it in app-private files.

If a verified model is bundled here during development, the predictor uses it when no app-private model is installed.
